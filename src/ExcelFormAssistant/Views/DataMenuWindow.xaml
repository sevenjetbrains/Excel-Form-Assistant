<Window x:Class="ExcelFormAssistant.Views.DataMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Données Excel"
        WindowStyle="None"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinWidth="260" MaxWidth="460" MaxHeight="600"
        Background="White"
        BorderBrush="#9A9A9A" BorderThickness="1"
        FontSize="13">
    <Window.Resources>
        <!-- Entrée du menu : bouton plat, jamais focalisable (le focus reste dans le formulaire) -->
        <Style x:Key="MenuItemButton" TargetType="Button">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="Transparent" Padding="10,5">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#DCEBFB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#C2DCF7" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter Property="Foreground" Value="#9A9A9A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#F0F0F0" BorderBrush="#D0D0D0" BorderThickness="0,0,0,1" Padding="10,6">
            <TextBlock x:Name="TitleText" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
        </Border>

        <TextBlock x:Name="EmptyText" DockPanel.Dock="Top" Visibility="Collapsed"
                   Margin="10,8" Foreground="#555555" TextWrapping="Wrap" />

        <ScrollViewer VerticalScrollBarVisibility="Auto" Focusable="False">
            <ItemsControl x:Name="ItemsList" Focusable="False" Margin="0,2">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource MenuItemButton}"
                                IsEnabled="{Binding CanCopy}"
                                Click="Item_Click"
                                ToolTip="{Binding Value}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22" />
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="ColumnName" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Shortcut}" Foreground="#888888" />
                                <TextBlock Grid.Column="1" Text="{Binding ColumnName}" Foreground="#555555"
                                           Margin="0,0,16,0" MaxWidth="160" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="2" Text="{Binding DisplayValue}" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" />
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
